1. 程序名称CSEFCRUD，意为CSharp，EntityFramework，增删查改。
2. 程序目的为练习各种基本技术，只需要能运行即可，没必要做成完善的产品，也就是可能有bug。
3. VS、.net版本：VS2015、.net4.5。
4. 模块：
  4.1 建立数据库StuDb，使用CodeFirst建立表Students,字段有ID int（设置成主键并自动增长）, Name char(20)，Sex Char(2), Age int, Pwd char(20), Discipline ncharar(50)（这个数据类型我怀疑写错了，而且要求里没说明这个字段的作用，因此不准备填充任何数据）。所有数据一律用英文。
  4.2 用三层构架和Linq封装CRUD数据操作，并有简单的测试用例（控制台运行）。
  4.3 在Winform上拖一个DataGridView、一个增加按钮、修改按钮、删除按钮、查询按钮。再增加一个界面可以填各种数据，功增加和修改用（增加不验证重复姓名，修改也不验证数据为空等）。并实现各个控件功能。
  4.4 实现WebService、WCF对学生身份的验证，控制台测试用例。
  4.5 实现WebAPI对学生身份的验证，控制台测试用例。
5. 验收和收费：
  5.1 以上共5个模块，依次每个功能做好后验收一次，每个功能收费10元。发exe测试，测试通过后再付费。
  5.2 如果预付费20元以上，则可以全部做完一次性发exe测试，测试通过后再付剩下的。
  5.3 付费方式支付宝leiyang-ge@163.com或者微信15184432110.
5. 完成时间
  原则上不应该早于2016-11-19日早上。否则时间越近bug越多。